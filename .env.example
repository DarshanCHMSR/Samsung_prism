# 🔥 NEW FIREBASE CONFIGURATION
# Update these environment variables with your new Firebase project details

# Project Configuration
FIREBASE_PROJECT_ID=your-new-project-id
GOOGLE_CLOUD_PROJECT=your-new-project-id

# Service Account
GOOGLE_APPLICATION_CREDENTIALS=./config/samsung-prism-banking-app-v2-firebase-adminsdk.json

# Firestore Configuration  
FIRESTORE_EMULATOR_HOST=  # Leave empty for production

# Optional: Firebase Auth Emulator (for development)
FIREBASE_AUTH_EMULATOR_HOST=  # Leave empty for production

# Agent Development Kit Specific
AGENT_FIREBASE_PROJECT_ID=your-new-project-id

# Security Keys (Generate new ones)
SECRET_KEY=your-new-secret-key-here
JWT_SECRET=your-new-jwt-secret-here

# API Configuration
GEMINI_API_KEY=your-gemini-api-key  # Keep existing if working

# Keystroke Auth Backend
KEYSTROKE_SERVER_URL=http://localhost:5000  # Keep existing

# CORS Origins (update if deploying)
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,https://your-new-domain.com